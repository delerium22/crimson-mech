---
- hosts: EC2_Servers
  tasks:
    - name: copying file with owner and permissions
      copy:
        src: FilesToSend/*
        dest: /tmp/testfile.txt
        owner: ec2-user
        group: ec2-user
        mode: 0750
